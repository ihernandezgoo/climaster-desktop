<Window x:Class="climaster.WeatherWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:climaster.Converters"
        Title="Weather Widget" 
        Height="400" Width="320"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <converters:GradientStringToBrushConverter x:Key="GradientConverter"/>
        <converters:UnixTimestampToDateTimeConverter x:Key="UnixTimeConverter"/>
    </Window.Resources>

    <!-- Widget Content -->
    <Border x:Name="WidgetBorder" 
            CornerRadius="20" 
            Margin="10"
            Opacity="{Binding Settings.WidgetOpacity}">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#2C3E50" Offset="0"/>
                <GradientStop Color="#34495E" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.4" Color="Black"/>
        </Border.Effect>

        <Border.RenderTransform>
            <ScaleTransform ScaleX="{Binding Settings.WidgetScale}" 
                          ScaleY="{Binding Settings.WidgetScale}"
                          CenterX="150" CenterY="200"/>
        </Border.RenderTransform>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="{Binding Settings.LocationName}" 
                          FontSize="18" 
                          FontWeight="SemiBold"
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentDescription}" 
                          FontSize="12" 
                          Foreground="#E0E0E0"
                          HorizontalAlignment="Center"
                          Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Main Weather Display -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding WeatherIcon}" 
                          FontSize="80" 
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                
                <TextBlock Text="{Binding CurrentTemperature}" 
                          FontSize="60" 
                          FontWeight="Bold"
                          Foreground="White"
                          HorizontalAlignment="Center"/>
                
                <TextBlock Text="{Binding CurrentDescription}" 
                          FontSize="16" 
                          Foreground="#E0E0E0"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Weather Details -->
            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Feels Like -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="🌡️" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Text="Sensación" 
                              FontSize="10" 
                              Foreground="#B0B0B0" 
                              HorizontalAlignment="Center"
                              Margin="0,4,0,2"/>
                    <TextBlock Text="{Binding FeelsLike}" 
                              FontSize="14" 
                              FontWeight="SemiBold"
                              Foreground="White" 
                              HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Humidity -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="💧" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Text="Humedad" 
                              FontSize="10" 
                              Foreground="#B0B0B0" 
                              HorizontalAlignment="Center"
                              Margin="0,4,0,2"/>
                    <TextBlock Text="{Binding Humidity}" 
                              FontSize="14" 
                              FontWeight="SemiBold"
                              Foreground="White" 
                              HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Wind -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="💨" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Text="Viento" 
                              FontSize="10" 
                              Foreground="#B0B0B0" 
                              HorizontalAlignment="Center"
                              Margin="0,4,0,2"/>
                    <TextBlock Text="{Binding WindSpeed}" 
                              FontSize="14" 
                              FontWeight="SemiBold"
                              Foreground="White" 
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Hourly Forecast -->
            <Border Grid.Row="3" 
                    Background="#20FFFFFF" 
                    CornerRadius="10" 
                    Margin="0,20,0,0"
                    Visibility="{Binding Settings.ShowHourlyForecast, Converter={StaticResource BoolToVisibilityConverter}}">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding WeatherData.Hourly}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10,10,10,10" Width="50">
                                    <TextBlock Text="{Binding DateTime, Converter={StaticResource UnixTimeConverter}, ConverterParameter='HH:mm'}" 
                                              FontSize="10" 
                                              Foreground="#B0B0B0"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Weather[0].Icon}" 
                                              FontSize="20" 
                                              HorizontalAlignment="Center"
                                              Margin="0,5,0,5"/>
                                    <TextBlock FontSize="12" 
                                              FontWeight="SemiBold"
                                              Foreground="White"
                                              HorizontalAlignment="Center">
                                        <Run Text="{Binding Temperature, StringFormat='{}{0:F0}'}"/>°
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Loading Indicator -->
            <Border Grid.Row="0" Grid.RowSpan="4"
                    Background="#80000000"
                    CornerRadius="20"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="40" HorizontalAlignment="Center"/>
                    <TextBlock Text="Cargando..." 
                              FontSize="16" 
                              Foreground="White"
                              Margin="0,10,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
