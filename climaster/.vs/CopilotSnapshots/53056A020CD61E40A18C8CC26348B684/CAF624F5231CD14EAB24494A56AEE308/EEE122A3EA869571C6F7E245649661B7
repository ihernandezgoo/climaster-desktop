<Window x:Class="climaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:climaster"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="CliMaster - Widget-aren Konfigurazioa" 
        Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="CanMinimize"
        ShowInTaskbar="False"
        Visibility="Collapsed">

    <Window.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="Konfigurazioa Erakutsi" Click="ShowConfig_Click"/>
            <MenuItem Header="Orain Eguneratu" Click="RefreshButton_Click"/>
            <Separator/>
            <MenuItem Header="Irten" Click="Exit_Click"/>
        </ContextMenu>
    </Window.Resources>

    <Grid>
        <!-- TaskbarIcon (diseinu bisualetik kanpo) -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="pack://application:,,,/icon.ico"
                        ToolTipText="CliMaster - Eguraldiaren Widget-a"
                        ContextMenu="{StaticResource TrayMenu}"
                        TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick"
                        Visibility="Visible">
            <tb:TaskbarIcon.TrayToolTip>
                <Border Background="#2C3E50" 
                        CornerRadius="5" 
                        BorderThickness="1" 
                        BorderBrush="#1A1A1A"
                        Padding="10">
                    <StackPanel>
                        <TextBlock Text="CliMaster" 
                                  FontWeight="Bold" 
                                  Foreground="White" 
                                  FontSize="12"/>
                        <TextBlock Text="{Binding Settings.LocationName}" 
                                  Foreground="#E0E0E0" 
                                  FontSize="10" 
                                  Margin="0,2,0,0"/>
                        <TextBlock Text="{Binding CurrentTemperature}" 
                                  Foreground="White" 
                                  FontSize="14" 
                                  FontWeight="SemiBold"
                                  Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </tb:TaskbarIcon.TrayToolTip>
        </tb:TaskbarIcon>

        <!-- Diseinu nagusia -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Goiburua -->
            <Border Grid.Row="0" Background="{StaticResource AccentBrush}" Padding="20">
                <StackPanel>
                    <TextBlock Text="⛅ CliMaster" 
                              FontSize="28" 
                              FontWeight="Bold"
                              Foreground="White"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Konfiguratu zure eguraldiaren widget-a" 
                              FontSize="14" 
                              Foreground="#E0E0E0"
                              HorizontalAlignment="Center"
                              Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Ezarpenen Panela -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel>
                    <!-- Kokapenaren Sekzioa -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="20" 
                            Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📍 Kokapena" 
                                      FontSize="18" 
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      Margin="0,0,0,15"/>

                            <!-- Nire Kokapena Erabili Botoia -->
                            <Button Content="📍 Nire Oraingo Kokapena Erabili" 
                                    Click="UseMyLocation_Click"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Margin="0,0,0,15"
                                    HorizontalAlignment="Stretch"
                                    Background="#4CAF50"/>

                            <CheckBox Content="Oraingo kokapena automatikoki erabili" 
                                     IsChecked="{Binding UseCurrentLocation}"
                                     Style="{StaticResource ModernCheckBoxStyle}"
                                     Margin="0,0,0,15"
                                     ToolTip="Aktibatuta badago, zure GPS kokapena automatikoki erabiliko da"/>

                            <!-- ComboBox de kokapena -->
                            <TextBlock Text="Aukeratu kokapena:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableLocations}"
                                     SelectedItem="{Binding SelectedLocation, Mode=TwoWay}"
                                     Style="{StaticResource ModernComboBoxStyle}"
                                     Margin="0,0,0,15"
                                     Height="40"
                                     FontSize="14"
                                     IsEnabled="{Binding UseCurrentLocation, Converter={StaticResource InverseBooleanConverter}}">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Padding" Value="10,8"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Name}" Value="──────────">
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Setter Property="Foreground" Value="#CCCCCC"/>
                                                <Setter Property="Cursor" Value="Arrow"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPredefined}" Value="False">
                                                <Setter Property="Background" Value="#F0F8FF"/>
                                            </DataTrigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DisplayText}" />
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <TextBlock Text="Kokapenaren izena:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Settings.LocationName, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    Margin="0,0,0,15"
                                    IsEnabled="{Binding UseCurrentLocation, Converter={StaticResource InverseBooleanConverter}}"/>

                            <TextBlock Text="Latitudea:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Settings.Latitude, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    Margin="0,0,0,15"
                                    IsEnabled="{Binding UseCurrentLocation, Converter={StaticResource InverseBooleanConverter}}"/>

                            <TextBlock Text="Longitudea:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Settings.Longitude, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    IsEnabled="{Binding UseCurrentLocation, Converter={StaticResource InverseBooleanConverter}}"/>
                        </StackPanel>
                    </Border>

                    <!-- Itxuraren Sekzioa -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="20" 
                            Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🎨 Itxura" 
                                      FontSize="18" 
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      Margin="0,0,0,15"/>

                            <TextBlock Text="Widget-aren opakutasuna:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Slider Grid.Column="0"
                                       Value="{Binding Settings.WidgetOpacity}" 
                                       Minimum="0.3" 
                                       Maximum="1.0" 
                                       TickFrequency="0.1"
                                       IsSnapToTickEnabled="True"
                                       Style="{StaticResource ModernSliderStyle}"/>
                                <TextBlock Grid.Column="1" 
                                          Text="{Binding Settings.WidgetOpacity, StringFormat='{}{0:P0}'}" 
                                          FontSize="12"
                                          VerticalAlignment="Center"
                                          Margin="10,0,0,0"/>
                            </Grid>

                            <TextBlock Text="Widget-aren tamaina:" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Slider Grid.Column="0"
                                       Value="{Binding Settings.WidgetScale}" 
                                       Minimum="0.5" 
                                       Maximum="1.5" 
                                       TickFrequency="0.1"
                                       IsSnapToTickEnabled="True"
                                       Style="{StaticResource ModernSliderStyle}"/>
                                <TextBlock Grid.Column="1" 
                                          Text="{Binding Settings.WidgetScale, StringFormat='{}{0:P0}'}" 
                                          FontSize="12"
                                          VerticalAlignment="Center"
                                          Margin="10,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Bistaratze Aukerak -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="20" 
                            Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="👁️ Bistaratze aukerak" 
                                      FontSize="18" 
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      Margin="0,0,0,15"/>

                            <CheckBox Content="Orduko iragarpena erakutsi" 
                                     IsChecked="{Binding Settings.ShowHourlyForecast}"
                                     Style="{StaticResource ModernCheckBoxStyle}"
                                     Margin="0,0,0,10"/>

                            <CheckBox Content="Eguneko iragarpena erakutsi" 
                                     IsChecked="{Binding Settings.ShowDailyForecast}"
                                     Style="{StaticResource ModernCheckBoxStyle}"/>
                        </StackPanel>
                    </Border>

                    <!-- Eguneratze Ezarpenak -->
                    <Border Background="White" 
                            CornerRadius="10" 
                            Padding="20">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🔄 Eguneratzea" 
                                      FontSize="18" 
                                      FontWeight="SemiBold"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      Margin="0,0,0,15"/>

                            <TextBlock Text="Eguneratze tartea (minutuak):" 
                                      FontSize="12" 
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Settings.RefreshInterval, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Ekintza Botoiak -->
            <Border Grid.Row="2" 
                    Background="White" 
                    BorderThickness="0,1,0,0" 
                    BorderBrush="#E0E0E0"
                    Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="1"
                            Content="Orain Eguneratu" 
                            Click="RefreshButton_Click"
                            Style="{StaticResource ModernButtonStyle}"
                            Margin="0,0,10,0"/>

                    <Button Grid.Column="2"
                            Content="Ezkutatu" 
                            Click="HideButton_Click"
                            Style="{StaticResource ModernButtonStyle}"/>
                </Grid>
            </Border>

            <!-- Karga Gainjartzea -->
            <Border Grid.Row="0" Grid.RowSpan="3"
                    Background="#CC000000"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="60" HorizontalAlignment="Center"/>
                    <TextBlock Text="Eguraldiaren datuak eguneratzen..." 
                              FontSize="18" 
                              Foreground="White"
                              Margin="0,20,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
